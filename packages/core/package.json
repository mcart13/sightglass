{
  "name": "@sightglass/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run -T tsc -p tsconfig.json",
    "typecheck": "yarn run -T tsc -p tsconfig.json --noEmit",
    "test": "yarn run -T vitest run",
    "test:e2e": "yarn run -T tsc -p tsconfig.json && yarn run -T playwright test --config=playwright.config.ts --reporter=line --workers=1",
    "lint": "echo 'lint not configured for @sightglass/core yet'"
  }
}
