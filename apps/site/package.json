{
  "name": "@sightglass/site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.1.0",
    "vite": "^7.1.12"
  },
  "scripts": {
    "dev": "vite --config vite.config.ts --host 127.0.0.1 --port 4174 --strictPort",
    "build": "vite build --config vite.config.ts && yarn run -T tsc -p tsconfig.json --noEmit",
    "typecheck": "yarn run -T tsc -p tsconfig.json --noEmit",
    "test": "echo 'test not configured for @sightglass/site yet'",
    "lint": "echo 'lint not configured for @sightglass/site yet'"
  }
}
